// import Header from './header';
import Navbar from './navbar';

const Layout = ({ children }) => {
    return (
        <>
            {/* <Header /> */}
            <Navbar />
        </>
    );
};

export default Layout;
